\n# Sprint: Active Users Source of Truth\n\n## Diagnóstico (causa-raiz)\n\nO hook `useOrganizationUsers` é a fonte usada por **15 telas operacionais** (Pipeline, Opportunities, EditOpportunityModal, OpportunitySidebar, DealParticipantsManager, CreateOpportunityModal, Create/EditActivityModal, ProposalParticipantsManager, WorkflowRuleModal, EditPipelineModal, Reports, Account, OTESellerAssignment, TeamMembersManager).\n\nEle filtra `organization_members.status='active'`, mas faz **UNION** com todos os `profiles.organization_id = org` (sem filtro de status/deleted_at) e usa essa união como `IN (...)` final. Resultado: os 11 membros `deleted` + 1 `suspended` retornam como \