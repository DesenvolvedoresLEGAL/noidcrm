# NOID RevenueOS — STAGING environment template (sanitized).
# Used by scripts/apply-migrations-staging.sh, scripts/staging-smoke-tests.sh
# and the tenant-isolation vitest suite. NEVER paste values from production
# (project ref urihdqturaebhiefwjnw) into this file.
#
# All values below MUST point to a Supabase project that is:
#   - a distinct project ref from production
#   - populated only with synthetic data
#   - not connected to real customer webhooks, Slack channels, or billing

TEST_SUPABASE_URL=https://STAGING-REF.supabase.co
TEST_SUPABASE_ANON_KEY=eyJhbGciOi...staging-anon...
TEST_SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...staging-service-role...
TEST_SUPABASE_DB_URL=postgresql://postgres:REPLACE_ME@db.STAGING-REF.supabase.co:5432/postgres
SUPABASE_STAGING_PROJECT_REF=STAGING-REF

# Guard for CI — the tenant-isolation workflow refuses to run against production.
PROD_SUPABASE_PROJECT_REF=urihdqturaebhiefwjnw
